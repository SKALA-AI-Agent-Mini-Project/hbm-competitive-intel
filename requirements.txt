# ── Core ───────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0
typing-extensions>=4.9.0

# ── LangChain / LangGraph (v1.0+ 필수) ────────────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langchain-huggingface>=0.1.0
langchain-text-splitters>=0.3.0
langchain-tavily>=0.1.0
langgraph>=0.2.50

# ── LLM ───────────────────────────────────────────────────────────────
openai>=1.50.0

# ── Embedding ─────────────────────────────────────────────────────────
sentence-transformers>=3.0.0   # HuggingFaceEmbeddings + CrossEncoder Reranker
torch>=2.0.0                   # CPU 전용: pip install torch>=2.0.0+cpu

# ── Retrieval ─────────────────────────────────────────────────────────
faiss-cpu>=1.8.0
rank-bm25>=0.2.2

# ── Web Search & Data Collection ──────────────────────────────────────
arxiv>=2.1.0                   # ArxivQueryRun (web_search_agent)

# ── Document Loading ──────────────────────────────────────────────────
pypdf>=4.0.0                   # PDF 로딩 (indexer.py)

# ── PDF Export (보고서 산출물) ─────────────────────────────────────────
weasyprint>=61.0               # 1순위: 한국어 테이블 완벽 지원
markdown2>=2.4.0               # Markdown → HTML 변환 (weasyprint용)

# ── Notebook ──────────────────────────────────────────────────────────
ipykernel>=6.0.0
ipython>=8.0.0
ipywidgets>=8.0.0              # tqdm 노트북 진행률 표시

# ── Optional: Samsung Careers 크롤러 ──────────────────────────────────
# playwright>=1.40.0           # pip install playwright && playwright install chromium

# ── Optional: Voyage AI 임베딩 (영문 특허·논문 특화) ─────────────────
# langchain-voyageai>=0.1.0    # pip install langchain-voyageai
# voyageai>=0.2.0              # VOYAGE_API_KEY 필요 (https://www.voyageai.com)